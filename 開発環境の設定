githubu上で共同で開発をする場合
まず開発リーダーがリポジトリを作成し、github上でメンバーを招待します。
次にリーダーのvscode上のターミナルで下記のコードを入力
git clone https://github.com/s2422092/Giikuten.git

これを入力したら共同で開発できるようになっているはずです。

次に開発を行うにあたってバージョンを揃える必要があります。
pythonのバージョンは
python3.12に統一する
    理由としてはデプロイの方法がわからない場合石橋先生のサーバーにデプロイを行えるようにするため

そのためにファイルを読み込みます
pip install -r requirements.txt
このコードを打つことによって最低限のライブラリをインストールすることができます。